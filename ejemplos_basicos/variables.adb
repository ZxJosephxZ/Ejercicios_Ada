with Ada.Text_IO;
use Ada.Text_IO;

--Para declarar una variable primero le damos un nombre y luego le asignamos el tipo
--El simbolo de asignacion "=" en Ada se representa con ":="

procedure variables is
a: Integer := 1;
b: String := "Hola";
c: Float := 5.5;
begin
put_line(b);
end variables;