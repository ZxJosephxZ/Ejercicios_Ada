with Ada.Text_IO;
use Ada.Text_IO;

procedure fija_flotante is
type mi_flotante is digits 10;--un flotante con 10 decimales
--delta se implementa para calculos cientificos
type precision is delta 0.01 range -100.0..100.0;--declara una precision de 0.01 dentro de un rango
begin
end fija_flotante;