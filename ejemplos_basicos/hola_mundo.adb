with Ada.Text_IO;
use Ada.Text_IO;

--El programa principal debe tener el mismo nombre que el archivo principal
--put_line es una funcion que nos permite mostrar texto por consola de salida
--Los archivos deben estar en minuscula de lo contrario el compilador les dara un warning y
--cuando quieran usar otros archivos no los encontrara
procedure hola_mundo is
begin
put_line("Hola mundo");
end hola_mundo;